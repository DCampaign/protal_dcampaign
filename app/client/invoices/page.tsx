import { OperationsShell, EmptyOperation } from '@/components/portal/operations-shell';
import { getCurrentClientContext } from '@/lib/queries/client-context';
export default async function InvoicesPage(){ const {supabase,clientId}=await getCurrentClientContext(); const invoices=supabase&&clientId?(await supabase.from('invoices').select('id,invoice_number,issue_date,due_date,total,currency,status,description').eq('client_id',clientId).order('issue_date',{ascending:false})).data??[]:[]; return <OperationsShell kicker="INVOICES" title="Billing, kept simple." description="Review invoices and payment status for your DCampaign account.">{invoices.length===0?<EmptyOperation>No invoices have been issued for your account yet.</EmptyOperation>:<div className="mt-8 overflow-x-auto rounded-2xl border border-white/10"><table className="w-full min-w-[600px] text-left text-sm"><thead className="bg-white/[.035] text-[.62rem] uppercase tracking-widest text-white/40"><tr><th className="px-5 py-4">Invoice</th><th>Issued</th><th>Due</th><th>Amount</th><th>Status</th></tr></thead><tbody className="divide-y divide-white/8">{invoices.map(i=><tr key={i.id}><td className="px-5 py-4 font-bold">{i.invoice_number}</td><td className="text-white/50">{i.issue_date}</td><td className="text-white/50">{i.due_date??'—'}</td><td className="font-bold">₹{Number(i.total??0).toLocaleString('en-IN')}</td><td className="text-white/50">{i.status}</td></tr>)}</tbody></table></div>}</OperationsShell>; }
