import { OperationsShell, EmptyOperation } from '@/components/portal/operations-shell';
import { getCurrentClientContext } from '@/lib/queries/client-context';
export default async function FilesPage(){ const {supabase,clientId}=await getCurrentClientContext(); const files=supabase&&clientId?(await supabase.from('files').select('id,name,original_filename,category,mime_type,size_bytes,created_at').eq('client_id',clientId).eq('visible_to_client',true).order('created_at',{ascending:false}).limit(50)).data??[]:[]; return <OperationsShell kicker="SHARED FILES" title="Everything your team has shared." description="Reports, creative, documents, and campaign files in one private library.">{files.length===0?<EmptyOperation>Your shared files will appear here when the DCampaign team uploads them.</EmptyOperation>:<div className="mt-8 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{files.map(file=><article key={file.id} className="glass-card rounded-2xl p-6"><p className="text-[.62rem] font-bold uppercase tracking-widest text-brand">{file.category}</p><h2 className="mt-4 truncate font-display font-bold">{file.name}</h2><p className="mt-2 text-xs text-white/40">{file.original_filename} · {(Number(file.size_bytes)/1024).toFixed(0)} KB</p><p className="mt-4 text-[.62rem] uppercase tracking-wider text-white/30">Uploaded {new Date(file.created_at).toLocaleDateString('en-IN')}</p></article>)}</div>}</OperationsShell>; }
