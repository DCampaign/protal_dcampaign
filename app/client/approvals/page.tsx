import { OperationsShell, EmptyOperation } from '@/components/portal/operations-shell';
import { getCurrentClientContext } from '@/lib/queries/client-context';
export default async function ApprovalsPage(){ const {supabase,clientId}=await getCurrentClientContext(); const approvals=supabase&&clientId?(await supabase.from('approvals').select('id,title,description,status,due_date,created_at,preview_url').eq('client_id',clientId).order('created_at',{ascending:false})).data??[]:[]; return <OperationsShell kicker="APPROVALS" title="Work waiting for your voice." description="Review requests from your team and keep campaigns moving with a clear response.">{approvals.length===0?<EmptyOperation>There are no approval requests waiting for you.</EmptyOperation>:<div className="mt-8 space-y-4">{approvals.map(item=><article key={item.id} className="glass-card rounded-2xl p-6 md:p-7"><div className="flex flex-col justify-between gap-4 sm:flex-row"><div><p className="text-[.62rem] font-bold uppercase tracking-widest text-brand">Approval request</p><h2 className="mt-2 font-display text-xl font-extrabold">{item.title}</h2></div><span className="h-fit rounded-full border border-brand/25 bg-brand/10 px-3 py-1.5 text-[.6rem] font-bold uppercase tracking-wider text-brand-light">{String(item.status).replaceAll('_',' ')}</span></div>{item.description&&<p className="mt-4 max-w-3xl text-sm leading-7 text-white/55">{item.description}</p>}<div className="mt-5 flex flex-wrap gap-6 text-xs text-white/40"><span>Requested {new Date(item.created_at).toLocaleDateString('en-IN')}</span>{item.due_date&&<span>Due {item.due_date}</span>}{item.preview_url&&<a href={item.preview_url} target="_blank" rel="noreferrer" className="font-bold text-brand-light hover:text-white">Open preview ↗</a>}</div>{item.status==='pending'&&<p className="mt-5 text-xs font-semibold text-brand-light">Response actions will be enabled once your Supabase authentication is connected.</p>}</article>)}</div>}</OperationsShell>; }
