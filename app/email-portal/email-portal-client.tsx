'use client';

import { useMemo, useState } from 'react';
import { CalendarClock, CheckCircle2, Clock3, Mail, Send, ShieldCheck } from 'lucide-react';
import { createSupabaseBrowserClient } from '@/lib/supabase/client';

export function EmailPortalClient() {
  const [loggedIn, setLoggedIn] = useState(false);
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [error, setError] = useState('');
  const [recipients, setRecipients] = useState('');
  const [subject, setSubject] = useState('');
  const [body, setBody] = useState('');
  const [batchSize, setBatchSize] = useState(50);
  const [interval, setInterval] = useState(30);
  const [scheduled, setScheduled] = useState(false);
  const emails = useMemo(() => recipients.split(/[\s,;]+/).map((x) => x.trim().toLowerCase()).filter((x, i, a) => /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(x) && a.indexOf(x) === i).slice(0, 450), [recipients]);
  const batches = useMemo(() => Array.from({ length: Math.ceil(emails.length / batchSize) }, (_, i) => emails.slice(i * batchSize, (i + 1) * batchSize)), [emails, batchSize]);

  async function login(event: React.FormEvent<HTMLFormElement>) {
    event.preventDefault();
    setError('');
    const supabase = createSupabaseBrowserClient();
    if (!supabase) { setError('Supabase authentication is not configured yet.'); return; }
    const result = await supabase.auth.signInWithPassword({ email, password });
    if (result.error || !result.data.user) { setError('The email address or password is incorrect.'); return; }
    const profile = await supabase.from('profiles').select('role,is_active').eq('user_id', result.data.user.id).maybeSingle();
    if (!profile.data?.is_active || !['admin', 'super_admin'].includes(profile.data.role)) { await supabase.auth.signOut(); setError('This account does not have email portal admin access.'); return; }
    setLoggedIn(true);
  }

  if (!loggedIn) return <section className="mx-auto grid max-w-6xl gap-12 px-6 pb-16 pt-32 md:grid-cols-[1.05fr_.95fr] md:items-center md:px-12"><div><p className="text-xs font-extrabold uppercase tracking-[0.28em] text-brand">DCAMPAIGN EMAIL WORKSPACE</p><h1 className="mt-5 font-display text-4xl font-extrabold tracking-tight sm:text-6xl">Send every message with control.</h1><p className="mt-5 max-w-xl text-base leading-7 text-white/65">Sign in to schedule individual delivery while protecting your daily sending limit.</p><div className="mt-8 flex items-center gap-3 text-sm text-white/55"><ShieldCheck size={18} className="text-brand" />Admin-only sending workspace</div></div><form onSubmit={login} className="rounded-3xl border border-white/10 bg-[#171719] p-7 sm:p-9"><div className="grid size-12 place-items-center rounded-2xl bg-[#2a1711] text-brand-light"><Mail size={23} /></div><p className="mt-7 text-xs font-extrabold uppercase tracking-[0.22em] text-brand">ADMIN ACCESS</p><h2 className="mt-3 font-display text-3xl font-extrabold">Sign in to email portal.</h2><div className="mt-7 space-y-4"><label className="block text-sm font-bold text-white/75">Admin email<input value={email} onChange={(e) => setEmail(e.target.value)} type="email" required className="mt-2 h-12 w-full rounded-xl border border-white/10 bg-[#111113] px-4 text-white outline-none focus:border-brand" placeholder="admin@dcampaign.com" /></label><label className="block text-sm font-bold text-white/75">Password<input value={password} onChange={(e) => setPassword(e.target.value)} type="password" required className="mt-2 h-12 w-full rounded-xl border border-white/10 bg-[#111113] px-4 text-white outline-none focus:border-brand" placeholder="Enter password" /></label></div>{error && <p className="mt-4 rounded-xl border border-red-400/20 bg-red-400/10 px-4 py-3 text-sm text-red-200">{error}</p>}<button className="mt-6 h-12 w-full rounded-xl bg-brand text-sm font-extrabold uppercase tracking-wider text-white hover:bg-brand-dark">Sign in</button></form></section>;

  return <section className="mx-auto max-w-7xl px-6 pb-16 pt-32 md:px-12"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-xs font-extrabold uppercase tracking-[0.28em] text-brand">EMAIL PORTAL / ADMIN</p><h1 className="mt-4 font-display text-4xl font-extrabold tracking-tight sm:text-5xl">Bulk email scheduler.</h1><p className="mt-3 text-sm leading-6 text-white/60">Queue one-to-one messages in controlled batches.</p></div><div className="rounded-xl border border-brand/30 bg-[#24130d] px-4 py-3 text-sm font-bold text-brand-light">Daily limit: 450 emails</div></div><div className="mt-8 grid gap-6 xl:grid-cols-[1.1fr_.9fr]"><div className="space-y-6"><div className="rounded-3xl border border-white/10 bg-[#171719] p-6"><h2 className="font-display text-xl font-extrabold">Campaign details</h2><label className="mt-5 block text-sm font-bold text-white/75">Recipient emails<textarea value={recipients} onChange={(e) => setRecipients(e.target.value)} className="mt-2 min-h-36 w-full rounded-xl border border-white/10 bg-[#111113] p-4 text-sm text-white outline-none focus:border-brand" placeholder="Paste one email per line, or separate with commas" /></label><p className="mt-2 text-xs text-white/45">{emails.length} valid recipients loaded (maximum 450 per day)</p><label className="mt-4 block text-sm font-bold text-white/75">Subject<input value={subject} onChange={(e) => setSubject(e.target.value)} className="mt-2 h-12 w-full rounded-xl border border-white/10 bg-[#111113] px-4 text-white outline-none focus:border-brand" placeholder="Campaign subject" /></label><label className="mt-4 block text-sm font-bold text-white/75">Message<textarea value={body} onChange={(e) => setBody(e.target.value)} className="mt-2 min-h-32 w-full rounded-xl border border-white/10 bg-[#111113] p-4 text-sm text-white outline-none focus:border-brand" placeholder="Write the email content" /></label></div><div className="rounded-3xl border border-white/10 bg-[#171719] p-6"><div className="flex items-center gap-3"><CalendarClock size={19} className="text-brand" /><h2 className="font-display text-xl font-extrabold">Delivery rules</h2></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-sm font-bold text-white/70">Emails per batch<input type="number" min="1" max="50" value={batchSize} onChange={(e) => setBatchSize(Math.min(50, Math.max(1, Number(e.target.value))))} className="mt-2 h-11 w-full rounded-xl border border-white/10 bg-[#111113] px-3 text-white" /></label><label className="text-sm font-bold text-white/70">Interval (minutes)<input type="number" min="5" value={interval} onChange={(e) => setInterval(Math.max(5, Number(e.target.value)))} className="mt-2 h-11 w-full rounded-xl border border-white/10 bg-[#111113] px-3 text-white" /></label></div><button onClick={() => setScheduled(true)} disabled={!emails.length || !subject || !body} className="mt-6 inline-flex h-12 items-center gap-2 rounded-xl bg-brand px-5 text-sm font-extrabold text-white hover:bg-brand-dark disabled:opacity-40"><Send size={17} />{scheduled ? 'Schedule updated' : 'Schedule campaign'}</button></div></div><aside className="rounded-3xl border border-white/10 bg-[#111113] p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-extrabold uppercase tracking-[0.2em] text-brand">QUEUE PREVIEW</p><h2 className="mt-2 font-display text-2xl font-extrabold">{batches.length ? batches.length + ' batches' : 'No batches yet'}</h2></div><Clock3 size={20} className="text-brand" /></div><p className="mt-3 text-sm leading-6 text-white/55">Up to {batchSize} emails every {interval} minutes, maximum 450 per day.</p><div className="mt-6 space-y-3">{batches.slice(0, 6).map((batch, i) => <div key={i} className="rounded-2xl border border-white/8 bg-[#171719] p-4"><div className="flex justify-between text-sm font-bold"><span>Batch {i + 1}</span><span className="text-brand">{batch.length}</span></div><p className="mt-2 truncate text-xs text-white/45">{batch.slice(0, 2).join(', ')}</p></div>)}{scheduled && <div className="mt-4 flex items-center gap-2 rounded-xl border border-brand/30 bg-[#24130d] px-4 py-3 text-sm text-brand-light"><CheckCircle2 size={17} />Campaign schedule saved locally.</div>}</div></aside></div><p className="mt-6 text-xs leading-5 text-white/35">Sending activates only after an approved SMTP or email API is connected. This planner enforces the delivery rules before integration.</p></section>;
}
