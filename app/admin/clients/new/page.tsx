import Link from 'next/link';
import { createClientAction } from '../actions';

const fieldClass = 'mt-2 h-12 w-full rounded-xl border border-white/10 bg-white/5 px-4 text-base text-white outline-none transition focus:border-brand';
export default function NewClientPage() {
  return <div className="mx-auto max-w-4xl"><Link href="/admin/clients" className="text-sm font-bold text-brand-light">← Clients</Link><p className="mt-8 text-sm font-extrabold uppercase tracking-widest text-brand">New organization</p><h1 className="mt-3 font-display text-4xl font-extrabold">Create a client workspace.</h1><p className="mt-3 text-base text-white/50">Add the company first. You can assign services and invite users on the next screen.</p><form action={createClientAction} className="mt-8 grid gap-5 rounded-3xl border border-white/8 bg-white/[0.025] p-6 sm:grid-cols-2 sm:p-8"><label className="text-sm font-bold">Company name<input name="companyName" required minLength={2} maxLength={140} className={fieldClass} /></label><label className="text-sm font-bold">Workspace slug<input name="slug" required pattern="[a-z0-9]+(?:-[a-z0-9]+)*" placeholder="acme-company" className={fieldClass} /></label><label className="text-sm font-bold">Website<input name="website" type="url" placeholder="https://example.com" className={fieldClass} /></label><label className="text-sm font-bold">Industry<input name="industry" className={fieldClass} /></label><label className="text-sm font-bold">Contact name<input name="contactName" className={fieldClass} /></label><label className="text-sm font-bold">Contact email<input name="contactEmail" type="email" className={fieldClass} /></label><label className="text-sm font-bold">Contact phone<input name="contactPhone" className={fieldClass} /></label><label className="text-sm font-bold">Package name<input name="packageName" className={fieldClass} /></label><label className="text-sm font-bold">Status<select name="status" defaultValue="onboarding" className={fieldClass}><option value="lead">Lead</option><option value="onboarding">Onboarding</option><option value="active">Active</option><option value="paused">Paused</option></select></label><div className="flex items-end"><button className="min-h-12 w-full rounded-xl bg-brand px-5 text-sm font-extrabold hover:bg-brand-dark">Create client</button></div></form></div>;
}
