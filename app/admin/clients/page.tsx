import Link from 'next/link';
import { createSupabaseServerClient } from '@/lib/supabase/server';

export default async function AdminClientsPage() {
  const supabase = await createSupabaseServerClient();
  const clients = supabase ? (await supabase.from('clients').select('id,company_name,slug,status,package_name,contact_email,updated_at').order('updated_at', { ascending: false })).data ?? [] : [];
  return <><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="text-sm font-extrabold uppercase tracking-widest text-brand">Clients</p><h1 className="mt-3 font-display text-4xl font-extrabold">Organizations</h1><p className="mt-3 text-base text-white/50">Manage access, services, and active client workspaces.</p></div><Link href="/admin/clients/new" className="inline-flex min-h-12 items-center justify-center rounded-xl bg-brand px-5 text-sm font-extrabold hover:bg-brand-dark">Add client</Link></div>{clients.length ? <div className="mt-8 overflow-hidden rounded-2xl border border-white/8"><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left"><thead className="bg-white/[0.04] text-xs uppercase tracking-wider text-white/45"><tr><th className="p-4">Company</th><th className="p-4">Package</th><th className="p-4">Contact</th><th className="p-4">Status</th><th className="p-4"><span className="sr-only">Open</span></th></tr></thead><tbody>{clients.map((client) => <tr key={client.id} className="border-t border-white/8 text-sm"><td className="p-4"><p className="font-bold">{client.company_name}</p><p className="mt-1 text-xs text-white/35">{client.slug}</p></td><td className="p-4 text-white/60">{client.package_name || '—'}</td><td className="p-4 text-white/60">{client.contact_email || '—'}</td><td className="p-4"><span className="rounded-full bg-white/8 px-2.5 py-1 text-xs font-bold capitalize text-white/65">{String(client.status).replace('_', ' ')}</span></td><td className="p-4 text-right"><Link href={`/admin/clients/${client.id}`} className="font-bold text-brand-light hover:text-white">Manage →</Link></td></tr>)}</tbody></table></div></div> : <div className="mt-8 rounded-2xl border border-dashed border-white/12 p-8"><h2 className="font-display text-xl font-bold">No clients yet</h2><p className="mt-2 text-base text-white/50">Create the first organization to begin assigning services and inviting users.</p></div>}</>;
}
